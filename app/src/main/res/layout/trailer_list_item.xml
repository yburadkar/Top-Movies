<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="4dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageButton
        android:id="@+id/play_button"
        android:layout_width="@dimen/trailer_icon_size"
        android:layout_height="@dimen/trailer_icon_size"
        android:src="@android:drawable/ic_menu_slideshow"
        android:visibility="visible"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:contentDescription="@string/play_trailer_button_content_desc"/>

    <TextView
        android:id="@+id/trailer_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toTopOf="@id/play_button"
        app:layout_constraintTop_toBottomOf="@id/play_button"
        app:layout_constraintLeft_toRightOf="@id/play_button"
        app:layout_constraintRight_toRightOf="parent"
        android:textSize="@dimen/trailer_name_textSize"
        tools:text="Trailer 1 Name"/>

</androidx.constraintlayout.widget.ConstraintLayout>